{
  "name": "gateway-kit",
  "version": "0.1.0",
  "description": "A lightweight, config-driven API gateway built on the Node standard library.",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0"
  }
}
